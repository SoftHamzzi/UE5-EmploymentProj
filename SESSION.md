# SESSION.md

세션 관리 전략: **STATUS 파일 앵커 방식**

세션을 구현/검증으로 나누지 않는다. 대신 STATUS 파일이 세션 간 상태를 전달하므로, 하나의 흐름으로 계속 진행한다.

---

## 구조

- `DOCS/Notes/04/GAS_STATUS.md` — 전체 단계 진행 상황 (어느 단계까지 완료됐는지)
- `DOCS/Notes/04/04_GAS_0X_XXX_STATUS.md` — 단계별 상세 상태 (Step별 완료 여부, 버그, 미완료 항목)

코드는 사용자가 직접 작성, Claude는 구현 방법을 단계 문서(`04_GAS_0X_XXX.md`)에 기술. 코드 수정 후 "STATUS 파일 정리해줘"라고 요청하면 Claude가 코드를 읽고 STATUS 파일을 갱신한다.

---

## 세션 시작 템플릿

```
@GAS_STATUS.md @04_GAS_0X_XXX_STATUS.md
Step X 진행.
```

새 세션을 열어도 이 두 파일만 읽으면 이전 상태를 정확히 복원한다.

---

## 규칙

1. **STATUS 파일이 진실의 원천** — 단계 문서(`04_GAS_0X_XXX.md`)는 예정 코드를 보여줄 뿐, 실제 구현 여부는 STATUS 파일로 판단한다
2. **코드 수정 후 매번 STATUS 파일을 갱신한다** — 미루면 다음 세션에서 복원 비용이 커진다
3. **단계가 완료되면** 해당 STATUS 파일 상단의 "전체 상태"를 완료로 바꾸고, `GAS_STATUS.md`의 체크박스를 갱신한다
4. **깃 태그는 단계 완료 시점에 찍는다**
